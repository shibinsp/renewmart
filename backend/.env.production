# Production Environment Configuration
# This file should be used in production deployment
# Set RENEWMART_ENV=production to use these settings

# Environment
RENEWMART_ENV=production
ENVIRONMENT=production

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false
RELOAD=false

# Security (use strong values in production)
SECRET_KEY=your-production-secret-key-change-this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database (use production database URL)
DATABASE_URL=postgresql://postgres:shibin@localhost:5432/renew

# CORS (restrict to your frontend domain)
ALLOWED_ORIGINS=["https://yourdomain.com"]

# Logging
LOG_LEVEL=WARNING

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/var/www/renewmart/uploads

# SSL/TLS (if using HTTPS)
# SSL_KEYFILE=/path/to/keyfile
# SSL_CERTFILE=/path/to/certfile